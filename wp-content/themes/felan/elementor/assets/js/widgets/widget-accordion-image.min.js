(function (i) {
  "use strict";
  var n = function (s, d) {
    var l = s.find(".right img"),
      i = s.find(".felan-accordion-image");
    i.children(".accordion-section:first-child")
      .children(".accordion-content")
      .css("display", "block");
    i.children(".accordion-section:first-child").addClass("active");
    i.on("click", ".accordion-header", function (i) {
      i = i || window.event;
      i.preventDefault();
      i.stopPropagation();
      var n = d(this).parent(".accordion-section"),
        e = d(this).parent(".accordion-section").index();
      e++;
      var t = s.find(".right img:nth-child(" + e + ")");
      var c = t.prevAll().get();
      var a = t.nextAll().get();
      if (c.length > 0) {
        var r = c.reverse();
        d.each(r, function (i, n) {
          i++;
          var e = i * 100 * -1;
          d(this).css("transform", "translateY(" + e + "%)");
        });
      }
      if (a.length > 0) {
        d.each(a, function (i, n) {
          i++;
          var e = i * 100;
          d(this).css("transform", "translateY(" + e + "%)");
        });
      }
      t.css("transform", "translateY(0)");
      l.css("opacity", "0");
      t.css("opacity", "1");
      if (n.hasClass("active")) {
        n.removeClass("active");
        n.children(".accordion-content").slideUp(300);
      } else {
        var o = d(this).parents(".felan-accordion-image").first();
        if (!o.data("multi-open")) {
          o.children(".active")
            .removeClass("active")
            .children(".accordion-content")
            .slideUp(300);
        }
        n.addClass("active");
        n.children(".accordion-content").slideDown(300);
      }
    });
    l.each(function (i) {
      if (i > 0) {
        var n = i * 100;
        d(this).css("transform", "translateY(" + n + "%)");
      }
    });
  };
  i(window).on("elementor/frontend/init", function () {
    elementorFrontend.hooks.addAction(
      "frontend/element_ready/felan-accordion-image.default",
      n
    );
  });
})(jQuery);
